; WAV call recordings can be several MB (uncompressed, 8kHz); the PHP
; defaults (2M/8M) reject them before they ever reach the /subir_archivo
; validation. Only honored by php-fpm/CGI SAPIs, not `php artisan serve`.
upload_max_filesize = 25M
post_max_size = 25M
